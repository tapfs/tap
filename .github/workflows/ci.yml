name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always

jobs:
  check:
    name: Check (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt
      - uses: Swatinem/rust-cache@v2
      - name: Install FUSE (Linux)
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y libfuse3-dev fuse3
      - name: Build (Linux)
        if: runner.os == 'Linux'
        run: cargo build --all-targets
      - name: Build (macOS, NFS only)
        if: runner.os == 'macOS'
        run: cargo build --no-default-features --features nfs --all-targets
      - name: Test (Linux)
        if: runner.os == 'Linux'
        run: cargo test
      - name: Test (macOS, NFS only)
        if: runner.os == 'macOS'
        run: cargo test --no-default-features --features nfs
      - name: Clippy (Linux)
        if: runner.os == 'Linux'
        run: cargo clippy --all-targets -- -D warnings
      - name: Clippy (macOS, NFS only)
        if: runner.os == 'macOS'
        run: cargo clippy --no-default-features --features nfs --all-targets -- -D warnings

  fmt:
    name: Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - run: cargo fmt -- --check
